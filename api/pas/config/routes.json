{
  "routes": [
    {
      "method": "GET",
      "path": "/pas",
      "handler": "pas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pas/count",
      "handler": "pas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pas/:id",
      "handler": "pas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pas",
      "handler": "pas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pas/:id",
      "handler": "pas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pas/:id",
      "handler": "pas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
